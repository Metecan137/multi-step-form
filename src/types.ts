export interface FormValues {
    name: string
    email: string
    phoneNumber: string
}

export interface SelectPlan {
    plan: string
    price: Number
}

export interface Ons {
    name: string
    price: number
}